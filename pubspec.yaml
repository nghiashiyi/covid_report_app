name: covid_app
description: Application for joining Flutter UI Challenge by Flutter Vietnam
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.3
  flutter_screenutil: ^2.3.1
  google_fonts: ^1.1.0
  lottie: ^0.5.1
  json_annotation: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
   - assets/